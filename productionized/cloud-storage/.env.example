# Example environment variables for cloud-storage MCP server
# Copy this file to .env and fill in your actual values

# Required: Google Cloud Storage bucket name
GCS_BUCKET=my-bucket-name

# Optional: Root directory prefix within the bucket
# All operations will be scoped to this directory
# GCS_ROOT_DIRECTORY=mcp-files

# Optional: Google Cloud project ID
# Uses default credentials if not set
# GCS_PROJECT_ID=my-project-id

# Optional: Path to service account JSON key file
# Uses default credentials if not set
# GCS_KEY_FILE=/path/to/service-account-key.json

# Optional: Tool groups to enable (comma-separated)
# Options: readonly, write, admin
# Default: all groups enabled
# ENABLED_TOOLGROUPS=readonly,write

# Optional: Skip bucket connectivity check at startup
# Useful for testing or when credentials are not immediately available
# SKIP_HEALTH_CHECKS=true
