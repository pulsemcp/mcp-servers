# Example environment variables for cloud-storage MCP server
# Copy this file to .env and fill in your actual values

# Required: Google Cloud Storage bucket name
GCS_BUCKET=my-bucket-name

# Optional: Root directory prefix within the bucket
# All operations will be scoped to this directory
# GCS_ROOT_DIRECTORY=mcp-files

# ============================================================================
# AUTHENTICATION OPTIONS (choose one method)
# ============================================================================

# --- Option 1: Individual credential environment variables (recommended) ---
# Useful when you don't want to store a JSON key file on disk
# All three are required when using this method

# GCS_PROJECT_ID=my-project-id
# GCS_CLIENT_EMAIL=my-service-account@my-project-id.iam.gserviceaccount.com
# GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Note: For GCS_PRIVATE_KEY, you can either:
# 1. Use the literal key with \n for newlines (as stored in the JSON file)
# 2. Or use actual newlines if your shell supports it

# --- Option 2: Service account JSON key file ---
# Path to the downloaded service account key file from GCP Console

# GCS_KEY_FILE=/path/to/service-account-key.json
# GCS_PROJECT_ID=my-project-id  # Optional with key file (often included in the file)

# --- Option 3: Application Default Credentials (ADC) ---
# If no credentials are provided, the SDK will use ADC
# This works automatically on GCE, Cloud Run, or after running:
#   gcloud auth application-default login

# GCS_PROJECT_ID=my-project-id  # Optional, but recommended

# ============================================================================
# OTHER OPTIONS
# ============================================================================

# Optional: Tool groups to enable (comma-separated)
# Options: readonly, write, admin
# Default: all groups enabled
# ENABLED_TOOLGROUPS=readonly,write

# Optional: Skip bucket connectivity check at startup
# Useful for testing or when credentials are not immediately available
# SKIP_HEALTH_CHECKS=true
