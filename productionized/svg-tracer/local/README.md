# SVG Tracer MCP Server (Local)

Local stdio implementation of the SVG Tracer MCP Server.

## Configuration

No environment variables are required. The server operates on the local filesystem.

## Usage

```bash
node build/index.js
```
