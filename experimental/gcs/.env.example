# Example environment variables for manual testing
# Copy this file to .env and fill in your actual values

# Authentication (choose one method):

# Option 1: Service account key file (recommended for CI/production)
GCS_SERVICE_ACCOUNT_KEY_FILE=/path/to/service-account-key.json

# Option 2: Service account key JSON inline (useful for Docker/env-based deployments)
# GCS_SERVICE_ACCOUNT_KEY_JSON={"type":"service_account","project_id":"...","private_key":"..."}

# Option 3: Application Default Credentials (ADC) - no env vars needed
# Run: gcloud auth application-default login

# Required: Google Cloud project ID
GCS_PROJECT_ID=your-project-id

# Optional: Constrain operations to a single bucket
# GCS_BUCKET=my-bucket

# Optional: Tool group configuration
# GCS_ENABLED_TOOLGROUPS=readonly,readwrite

# Optional: Individual tool configuration
# GCS_ENABLED_TOOLS=list_buckets,get_object
# GCS_DISABLED_TOOLS=delete_bucket

# Optional: Skip health checks (useful for testing)
# SKIP_HEALTH_CHECKS=true
