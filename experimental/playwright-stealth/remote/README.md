# Remote Transport

This directory is reserved for future HTTP transport implementation.

The `local/` directory contains the stdio transport implementation which is suitable for local use with Claude Desktop.
