# Claude Code Agent MCP Server Configuration

# Path to your trusted servers list (markdown file with server descriptions)
TRUSTED_SERVERS_PATH=./servers.md

# Path to your server configurations (JSON file with MCP server configs)
SERVER_CONFIGS_PATH=./servers.json

# Path to Claude CLI executable (optional, defaults to 'claude')
CLAUDE_CODE_PATH=claude

# Path to secrets file for server credentials (optional)
SERVER_SECRETS_PATH=

# Base directory for agent workspaces (optional, defaults to /tmp/claude-agents)
CLAUDE_AGENT_BASE_DIR=/tmp/claude-agents

# Logging level (optional, defaults to 'info')
# Options: error, warn, info, debug
CLAUDE_AGENT_LOG_LEVEL=info