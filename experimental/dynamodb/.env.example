# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Optional: Custom DynamoDB endpoint (for local development)
# DYNAMODB_ENDPOINT=http://localhost:8000

# Optional: Tool access control
# DYNAMODB_ENABLED_TOOL_GROUPS=readonly,readwrite
# DYNAMODB_ENABLED_TOOLS=get_item,query_items
# DYNAMODB_DISABLED_TOOLS=delete_table

# Optional: Table access control (comma-separated list)
# DYNAMODB_ALLOWED_TABLES=Users,Orders,Products

# Optional: Skip startup health checks
# SKIP_HEALTH_CHECKS=true
