# Example environment variables for SSH MCP Server
# Copy this file to .env and fill in your actual values

# Required: SSH server connection details
SSH_HOST=192.168.1.100
SSH_USERNAME=deploy

# Optional: SSH port (default: 22)
SSH_PORT=22

# Authentication - choose one method:

# Method 1: SSH Agent (recommended for passphrase-protected keys)
# The SSH_AUTH_SOCK is usually auto-detected from your environment
# SSH_AUTH_SOCK=/path/to/ssh/agent/socket

# Method 2: Private key file
# SSH_PRIVATE_KEY_PATH=~/.ssh/id_ed25519
# SSH_PASSPHRASE=your-passphrase-if-key-is-encrypted

# Optional: Connection timeout in milliseconds (default: 30000)
# SSH_TIMEOUT=30000

# Optional: Tool groups to enable (default: all)
# ENABLED_TOOLGROUPS=readonly,write,admin
