# Required: GCS bucket name
GCS_BUCKET=your-bucket-name

# Optional: Google Cloud project ID (uses default if not set)
GCS_PROJECT_ID=your-project-id

# Authentication Option 1: Inline credentials (recommended)
GCS_CLIENT_EMAIL=sa-name@project.iam.gserviceaccount.com
GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Authentication Option 2: Key file path (alternative to inline credentials)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: Root path prefix - restricts access to files under this path
GCS_ROOT_PATH=uploads/

# Optional: Whether to make uploaded files publicly accessible (default: false)
GCS_MAKE_PUBLIC=false

# Optional: Enable specific tool groups (default: all)
# Options: readonly, readwrite (or both comma-separated)
ENABLED_TOOLGROUPS=readonly,readwrite
