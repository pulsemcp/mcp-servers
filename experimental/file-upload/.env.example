# Required: GCS bucket name
GCS_BUCKET=your-bucket-name

# Optional: Google Cloud project ID (uses default if not set)
GCS_PROJECT_ID=your-project-id

# Optional: Path to service account key file (uses default credentials if not set)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: Base path prefix for uploads (e.g., "screenshots/")
GCS_BASE_PATH=

# Optional: Whether to make uploaded files publicly accessible (default: true)
GCS_MAKE_PUBLIC=true
