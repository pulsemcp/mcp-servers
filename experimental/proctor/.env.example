# Proctor MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: API key for PulseMCP Proctor API
PROCTOR_API_KEY=your-api-key-here

# Optional: Base URL for Proctor API (defaults to https://admin.pulsemcp.com)
# PROCTOR_API_URL=https://admin.pulsemcp.com

# Optional: Comma-separated list of enabled tool groups
# Available groups: exams, exams_readonly, machines, machines_readonly
# Default: exams,machines (all tools enabled)
# TOOL_GROUPS=exams,machines
